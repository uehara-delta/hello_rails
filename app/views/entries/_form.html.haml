= form_with(model: [blog, entry], local: true, class: "uk-form-stacked") do |form|
  .uk-margin
    - if entry.errors.any?
      #error_explanation
        %h2= "#{pluralize(entry.errors.count, "error")} prohibited this entry from being saved:"

        %ul
          - entry.errors.full_messages.each do |message|
            %li= message

  .uk-margin
    = form.label :title, class: "uk-form-label"
    .uk-form-controls
      = form.text_field :title, class: "uk-input"

  .uk-margin
    = form.label :body, class: "uk-form-label"
    .uk-form-controls
      = form.text_area :body, class: "uk-textarea"

  .uk-margin
    .actions
      = form.submit "Save", class: "uk-button uk-button-default uk-button-small"
